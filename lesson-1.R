## Configure git

system("git config --global user.name 'Melissa Day' ")
# system("git config --global user.name ...")
system("git config --global user.email 'mdaylite@gmail.com' ")
# system("git config --global user.email ...")

## Set a new origin URL

# system('git remote set-url origin https://github.com/joelcreswell/promisezones.git') 
system('git remote set-url origin https://github.com/Marshlight/SESYNC-handouts.git') 
# system('git remote set-url origin ...')
